import baseConfig from '../ilib-common-config/jest-esm.config.js';

const config = {
    ...baseConfig,
    displayName: {
        name: "ilib-lint",
        color: "blackBright",
    },
};

export default config;
