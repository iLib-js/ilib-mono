---
"ilib-assemble": patch
---

Support the flat lib/ and locale/ directory layout that ilib uses when published to npm, in addition to the js/lib and js/data/locale layout of the ilib source repository
