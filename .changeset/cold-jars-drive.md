---
"ilib-loctool-ghfm": minor
---

Now supports filename extensions gleaned from the mappings - if the config includes mappings like "\*_/_.mdx", then the
file name extensions in that mapping are added automatically
to the list of valid extensions that this plugin handles
