import { jestConfig as baseConfig } from "ilib-internal";

const config = {
    ...baseConfig,
    displayName: {
        name: "ilib-lint",
        color: "blackBright",
    },
};

export default config;
