# loctool

## 3.0.0

### Major Changes

- Initial loctool 3 package scaffold. This is a TypeScript rewrite of loctool, developed in `packages/loctool3` so that loctool 2 can continue in `packages/loctool`. The workspace package is named `loctool3` (CLI: `loctool`, version 3.0.0) until version 2 is retired from this repo.
