import baseConfig from '../ilib-common-config/jest.config.js';

const config = {
    ...baseConfig,
    displayName: {
        name: "ilib-lint",
        color: "blackBright",
    },
};

export default config;
